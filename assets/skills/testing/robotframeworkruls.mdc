---
description: "Robot Framework自动化用例编写规范，包含用户关键字、变量管理、JSONPath使用及持续优化指南"
alwaysApply: false
---
# Robot Framework自动化用例规则

本规则定义了Robot Framework自动化用例的编写规范，涵盖用户关键字库（`Keywords.robot`）、变量库（`Variables.robot`）的使用规范以及用例编写风格要求。

## 主要任务

编写自动化用例时，需要严格遵循以下规则和最佳实践。

## 重要事项
- 不要轻易修改当前Variables.robot存在的变量,除非是本次添加的
- 修改存量的用例时，需要了解原本逻辑
- 优先使用现有接口查询数据，避免直接查询数据库（除非接口无法满足需求）
- **文档维护规则**：每次修改文档后必须整理一次整体文档，检查并消除重复内容，优化不当描述，确保文档结构清晰、内容精炼

## 用例编写规范

### [Documentation] 标签编写要求

新写或更新用例时，必须根据实际情况更新 `[Documentation]` 标签内容，用于对应TAPD用例，后续会根据此内容生成TAPD用例。

#### 格式要求

`[Documentation]` 必须包含三个部分，且必须在同一行，不要换行：

```
[Documentation]    【预置条件】<预置条件描述> 【操作步骤】<操作步骤描述> 【预期结果】<预期结果描述>
```

#### 内容编写原则

1. **预置条件**：描述执行用例前需要满足的前提条件
   - 例如：已创建商户、已配置渠道信息、已准备测试数据等

2. **操作步骤**：用文字描述执行了什么操作，而不是流水账式地记录调用的接口或关键字
   - ✅ 正确示例：提交商户状态变更申请，将商户状态从"正常"变更为"暂停"
   - ❌ 错误示例：调用QueryMerchInfo接口查询商户信息，调用UpdateMerchStatus接口更新状态

3. **预期结果**：描述用例执行后应该达到的结果
   - 例如：商户状态成功变更为"暂停"，返回成功提示信息等

#### 编写示例

```robotframework
*** Test Cases ***
商户状态变更-正常变暂停
    [Documentation]    【预置条件】已存在状态为"正常"的商户 【操作步骤】提交商户状态变更申请，将商户状态从"正常"变更为"暂停" 【预期结果】商户状态成功变更为"暂停"，系统返回变更成功提示
    [Tags]    中    未评审
    # 用例步骤...
```

#### 注意事项

- 描述要基于实际业务场景，用业务语言而非技术实现细节
- 三个部分必须在一行内完成，使用中文方括号【】标识
- 内容要简洁明了，能够清晰表达用例的业务意图
- 避免将接口调用、关键字名称等技术细节写入描述中
- 描述内容应与TAPD用例保持一致，便于后续自动生成

## JSONPath 支持规则

### 官方文档参考

JSONPath 是一种用于查询 JSON 数据的语言，最初由 Stefan Gössner 在 2007 年提出，并于 2024 年被 IETF 标准化为 RFC 9535。

**官方文档链接：**
- **IETF RFC 9535（标准规范）**：https://datatracker.ietf.org/doc/html/rfc9535
- **Stefan Gössner 原始文章**：https://goessner.net/articles/JsonPath/
- **RFC Editor**：https://www.rfc-editor.org/rfc/rfc9535.html

### 自定义库支持 JSONPath

自定义库 `Resp Dict Value` 和 `Resp Dict Compare` 完全支持 JSONPath 表达式，可用于从响应数据中提取和比较值。

### JSONPath 基本语法

JSONPath 表达式以 `$` 开头，表示根节点。常用语法包括：

- `$` - 根节点
- `$.property` - 访问根节点的属性
- `$.array[index]` - 访问数组元素（索引从0开始）
- `$.array[*]` - 访问数组所有元素（通配符）
- `$..property` - 递归下降，匹配所有层级的指定属性
- `$.array[?(@.property == 'value')]` - 使用过滤器表达式筛选数组元素
- `$.*` - 匹配根节点下的所有属性

### JSONPath 过滤器表达式

过滤器表达式格式：`[?(@.property operator value)]`，用于筛选符合条件的数组元素。

#### 支持的操作符

- `==` - 等于（注意：数字1不等于字符串'1'）
- `!=` - 不等于
- `<` - 小于
- `<=` - 小于等于
- `>` - 大于
- `>=` - 大于等于
- `=~` - 正则表达式匹配，例如：`[?(@.name =~/foo.*?/i)]`
- `in` - 属于，例如：`[?(@.size in ['S', 'M'])]`
- `nin` - 不属于（排除）
- `size` - 大小匹配（数组或字符串的长度）
- `empty` - 判空（Null 或空值）

#### 逻辑运算符

- `&&` - 逻辑与（AND）
- `||` - 逻辑或（OR）
- `!` - 逻辑非（NOT）

### JSONPath 使用示例

#### 示例1：基本过滤
```robotframework
${wechat_merch_no}    Resp Dict Value    $.chan_merch_info_list[?(@.status == '1' && @.channel_id == '10011')].chan_merch_no
```

此示例从 `chan_merch_info_list` 数组中筛选出 `status` 等于 '1' 且 `channel_id` 等于 '10011' 的元素，然后提取其 `chan_merch_no` 属性。

#### 示例2：多条件过滤
```robotframework
${result}    Resp Dict Value    $.data[?(@.type == 'active' && @.count > 10)].id
```

#### 示例3：正则表达式匹配
```robotframework
${matched}    Resp Dict Value    $.items[?(@.name =~/^test.*/i)].value
${books}    Resp Dict Value    $..book[?(@.author =~ /.*REES/i)]
```

说明：`$..book` 使用递归下降语法，会在所有层级中查找 `book` 数组；正则表达式 `/.*REES/i` 中的 `i` 表示不区分大小写。

#### 示例4：使用 in 操作符
```robotframework
${filtered}    Resp Dict Value    $.products[?(@.status in ['active', 'pending'])].name
```

### 使用建议

1. **复杂过滤条件**：使用 `&&` 和 `||` 组合多个条件
2. **字符串比较**：注意使用引号包裹字符串值，如 `'value'`
3. **数字比较**：数字值不需要引号，如 `@.count > 10`
4. **嵌套属性**：可以使用点号访问嵌套属性，如 `@.user.profile.name`
5. **数组索引**：在过滤后可以使用索引访问特定元素，如 `[0]` 获取第一个匹配项
6. **递归搜索**：使用 `..` 可以在所有层级中搜索，适用于不确定嵌套深度的场景
7. **正则表达式**：正则表达式格式为 `/pattern/flags`，常用标志 `i`（不区分大小写）、`g`（全局匹配）

### 注意事项

- JSONPath 表达式中的字符串值必须用单引号或双引号包裹
- 数字和布尔值不需要引号
- 过滤器表达式中的 `@` 表示当前正在评估的数组元素
- 使用 `Resp Dict Compare` 时，JSONPath 表达式同样适用
- 正则表达式支持可能因实现而异，建议参考具体库的文档
- 递归下降 `..` 会搜索所有匹配的路径，可能返回多个结果

## 存量业务-商户系统用例编写规范

### 用例结构规范

#### 标签使用

1. **`[Documentation]`**：必须包含【预置条件】【操作步骤】【预期结果】三部分，且在同一行
2. **`[Tags]`**：标记用例优先级（高/中/低）和评审状态（未评审/已评审）
3. **`[Timeout]`**：设置用例超时时间，通常为 `5 minutes`
4. **`[Setup]`**：用例级别的前置准备，使用 `Run Keyword And Ignore Error` 处理可能失败的操作
5. **`[Teardown]`**：用例级别的清理工作，确保测试环境恢复
6. **`Suite Setup`**：套件级别的前置准备，用于准备测试数据

#### 用例命名规范

- 使用中文描述业务场景
- 命名格式：`业务操作_具体场景` 或 `业务功能_操作类型`
- 示例：`商户绑定套餐_开关授权收款套餐`、`电签首次入网门店认证并加机`

### 编写风格

#### 注释使用

- 使用 `Comment` 关键字添加步骤说明，描述操作意图而非技术实现
- 在关键步骤前后添加注释，说明业务逻辑
- 示例：`Comment    先查询子商户详情`、`Comment    修改后数据比对`

#### 变量管理

- 使用 `Set Variable` 定义局部变量
- 使用 `Set Suite Variable` 定义套件级变量，供后续用例使用
- 使用 `Set Test Variable` 定义测试用例级变量，仅在当前用例内可见
- 使用 `Set Global Variable` 定义全局变量，谨慎使用，避免状态耦合
- 使用 `${{}}` 进行Python表达式计算
- 示例：`${turn_on}    Set Variable    1`、`Set Suite Variable    ${micro_term_no}`

**变量作用域选择原则**：
- **局部变量**（`Set Variable`）：仅在当前关键字或用例步骤内有效，优先使用
- **测试变量**（`Set Test Variable`）：在当前测试用例内可见，适用于用例内多步骤共享
- **套件变量**（`Set Suite Variable`）：在当前测试套件内可见，适用于套件内用例间共享
- **全局变量**（`Set Global Variable`）：在所有测试和套件中可见，应谨慎使用，避免测试间相互影响

#### BuiltIn库使用规范

**优先使用BuiltIn库关键字**：
- 优先使用 BuiltIn 库提供的标准关键字，避免重复实现已有功能
- 禁止重复造轮子，优先查找 BuiltIn 库中是否已有对应关键字
- 在自定义关键字中获取 Robot Framework 或其他库实例应通过 `Get Library Instance` 而不是重新导入

**控制流关键字**：
- 使用 `Return From Keyword` 或 `Return From Keyword If` 在关键字中提前返回，避免复杂嵌套
- 支持返回多个值，可与 `[Return]` 并用
- 示例：
  ```robotframework
  查找索引
      [Arguments]    ${element}    @{items}
      ${index}    Set Variable    ${0}
      FOR    ${item}    IN    @{items}
          Return From Keyword If    '${item}' == '${element}'    ${index}
          ${index}    Evaluate    ${index} + 1
      END
      Return From Keyword    ${-1}
  ```

**命名参数和默认值**：
- 使用 `param=value` 语法定义默认值参数
- 避免位置参数放在命名参数之后
- 支持可变参数 `@{args}` 和命名参数字典 `&{kwargs}`，提升关键字通用性
- 示例：
  ```robotframework
  通用关键字
      [Arguments]    ${必填参数}    ${可选参数}=默认值    @{可变参数}    &{命名参数}
      Log    必填参数: ${必填参数}
      Log    可选参数: ${可选参数}
      FOR    ${arg}    IN    @{可变参数}
          Log    可变参数: ${arg}
      END
      FOR    ${key}    ${value}    IN    &{命名参数}
          Log    ${key} = ${value}
      END
  ```

**日志和调试**：
- 使用 `Log` 记录关键步骤信息，明确日志级别（INFO/DEBUG/WARN/ERROR）
- 使用 `Log Many` 一次性记录多个变量值
- 使用 `Comment` 添加不执行的说明性注释
- 在关键步骤或调试点插入日志，提高可调试性
- 示例：`Log    开始执行商户查询    level=INFO`、`Log Many    ${merch_no}    ${status}    ${result}`

**列表操作**：
- 使用 `Get Length` 获取列表长度
- 使用 `Get From List` 获取列表元素
- 使用 `Append To List` 添加元素
- 使用 `Create List` 创建列表
- 示例：
  ```robotframework
  ${length}    Get Length    ${list}
  ${item}    Get From List    ${list}    0
  Append To List    ${list}    new_item
  ```

**字符串操作**：
- 使用 `Get Substring` 获取子字符串
- 使用 `Replace String` 替换字符串
- 使用 `Split String` 分割字符串
- 使用 `Should Contain` 验证包含关系
- 示例：
  ```robotframework
  ${sub}    Get Substring    ${str}    0    5
  ${new_str}    Replace String    ${str}    old    new
  @{parts}    Split String    ${str}    ,
  ```

**等待机制**：
- 使用 `Wait Until Keyword Succeeds` 等待关键字成功执行，适用于异步操作
- 使用 `Sleep` 固定时间等待
- 示例：
  ```robotframework
  Wait Until Keyword Succeeds    30s    2s    关键字名称    参数
  Sleep    3s
  ```

**评估表达式**：
- 使用 `Evaluate` 执行 Python 表达式，注意使用 `${{}}` 语法避免变量替换问题
- 示例：
  ```robotframework
  ${result}    Evaluate    ${a} + ${b}
  ${timestamp}    Evaluate    int(time.time())
  ```

**条件执行**：
- 使用 `Run Keyword If` 根据条件执行不同关键字，避免复杂嵌套
- 使用 `Run Keyword Unless` 执行否定条件
- 示例：
  ```robotframework
  Run Keyword If    ${condition}    Log    条件为真
  Run Keyword Unless    ${condition}    Log    条件为假
  ```

**关键字组合**：
- 使用 `Run Keywords` 组合多个关键字，用 `AND` 连接，实现多步骤原子操作
- 示例：
  ```robotframework
  Run Keywords    步骤1    AND    步骤2    AND    步骤3
  ```

**库实例获取**：
- 使用 `Get Library Instance` 获取已导入库的实例，保持状态一致性
- 避免在自定义库中重新导入或实例化库，可能导致状态不一致
- 示例：在 Python 库中使用 `BuiltIn().get_library_instance('SeleniumLibrary')` 获取已加载的库实例

**库搜索顺序控制**：
- 当多个库或资源中存在同名关键字时，使用 `Set Library Search Order` 明确优先级
- 优先列出项目自定义资源或库，避免关键字冲突
- 示例：`Set Library Search Order    MyCustomLib    SharedLib    BuiltIn`

**错误处理**：
- **注意**：当前平台是 Robot Framework 3.2.2，`TRY / EXCEPT / FINALLY` 语法仅支持 Robot Framework 5+，因此只展示不推荐使用
- 兼容当前版本时，应使用 `Run Keyword And Expect Error` 或 `Run Keyword And Ignore Error`
- 使用 `Run Keyword And Ignore Error` 处理可能失败的前置或清理操作
- 示例：
  ```robotframework
  ${status}    ${result}    Run Keyword And Ignore Error    可能失败的操作
  Run Keyword If    '${status}' == 'FAIL'    Log    操作失败: ${result}
  ```
- TRY/EXCEPT/FINALLY 语法示例（仅作参考，当前平台不支持）：
  ```robotframework
  TRY
      执行可能失败的操作
  EXCEPT    AS    ${error}
      Log    操作失败: ${error}
      执行清理操作
  FINALLY
      最终清理
  END
  ```

**变量替换**：
- 在自定义库中使用 `BuiltIn.replace_variables()` 处理变量替换
- 支持访问当前可用变量并处理字符串中包含的变量表达式（如 `${VAR}`）
- 适用于需要动态解析变量的场景

**返回值处理**：
- Robot Framework 5+ 推荐使用 `RETURN` 语法返回值
- 兼容旧版本时，可使用 `[Return]` 或 `Return From Keyword`
- 支持返回多个值，用多个空格分隔

### 数据验证方法

#### 响应验证

1. **`Resp Dict Compare`**：验证响应结构，支持JSONPath表达式
   - 基本用法：`Resp Dict Compare    {"ret_code":"00","ret_msg":"处理成功"}`
   - JSONPath用法：`Resp Dict Compare    {"$.rows[?(@.merch_no=='${merch_no}')].status":"1","ret_code":"00"}`

2. **`Resp Dict Value`**：使用JSONPath提取响应数据
   - 示例：`${package_id}    Resp Dict Value    $.rows[0].package_info_list[?(@.package_name=='${name}')].package_id`
   - **重要提示**：当使用 `$.data[*].field_name` 提取数组字段时，如果数组中只有一个元素，`Resp Dict Value` 可能返回字符串而不是列表。需要判断类型并转换为列表：
   ```robotframework
   ${result}    Resp Dict Value    $.data[*].suffix_name
   ${length_result}    ${length_value}    Run Keyword And Ignore Error    Get Length    ${result}
   ${str_result}    Run Keyword And Ignore Error    Convert To String    ${result}
   ${str_length}    Get Length    ${str_result[1]}
   ${is_list}    Set Variable If    '${length_result}' == 'PASS' and '${str_result[0]}' == 'PASS' and ${length_value} != ${str_length}    ${True}    ${False}
   @{result_list}    Run Keyword If    ${is_list}    Set Variable    ${result}    ELSE    Create List    ${result}
   ```
   - **类型判断原理**：通过比较变量的长度和其字符串表示的长度来判断类型。如果长度不相等，说明是列表；如果相等，说明是字符串。这样可以避免在 Python 表达式中直接使用变量值导致的语法错误。

3. **`Resp Value`**：提取简单字段值
   - 示例：`${merch_basic}    Resp Value    merch_basic`

#### 数据库验证

- 使用 `通用查询数据库` 验证数据持久化
- 对比接口返回数据与数据库数据的一致性
- 示例：`${ob_data}    通用查询数据库    ${sd_ob_merch_user}    T_MERCH_ADDITION    T.MERCH_NO = '${merch_no}'    oceanbase    T.SIMPLE_NAME`

#### 断言使用

**常用断言关键字**：
- `Should Be Equal` - 验证值相等
- `Should Not Be Equal` - 验证值不相等
- `Should Not Be Empty` - 验证非空
- `Should Be True` - 验证条件为真
- `Should Not Be True` - 验证条件为假
- `Should Contain` - 验证包含关系
- `Should Not Contain` - 验证不包含关系

**断言使用示例**：
```robotframework
Should Be Equal    ${actual}    ${expected}    消息不匹配
Should Not Be Empty    ${result}    结果为空
Should Be True    ${condition}    条件不满足
```

### 业务处理模式

> **注意**：详细的业务编写规范请参考"核心业务编写规范"章节。本节提供简要的业务处理模式概述。

#### 变更类用例模式

**存量业务模式**：采用"修改-验证-恢复"的三段式模式（详见"核心业务编写规范-变更业务"）

**V4.0商户系统模式**：采用"申请单-提交-审核-验证-恢复"的五段式模式（详见"核心业务编写规范-变更业务"）

#### 查询类用例模式

直接查询并验证结果（详见"核心业务编写规范-查询业务"）

#### 报备类用例模式

调用报备接口并验证数据库（详见"核心业务编写规范-报备业务"）

#### 终端类用例模式

**存量业务模式**：加机/撤机操作的标准流程（详见"核心业务编写规范-加机业务"）

**V4.0商户系统模式**：终端加机操作的标准流程（详见"核心业务编写规范-加机业务"）

### 异步处理技巧

#### 等待机制

- 使用 `Sleep` 处理异步操作等待
- 变更类操作通常等待 `10` 秒（自动审核需要时间）
- 入网类操作通常等待 `${sd_sleep_time}` 秒（根据配置更灵活）
- 变更审核类操作通常等待 `${sd_change_auth_sleeptime}` 秒（根据配置更灵活）
- 查询类操作通常等待 `2-5` 秒
- 报备类操作通常等待 `3-5` 秒

#### 等待关键字

- 使用 `Wait Until Keyword Succeeds` 等待关键字成功执行，适用于异步操作
- 示例：
  ```robotframework
  Wait Until Keyword Succeeds    30s    2s    查询状态    ${merch_no}
  ```

#### 数据准备

- 使用 `Suite Setup` 准备套件级别的测试数据
- 使用 `[Setup]` 准备用例级别的测试数据
- 使用 `Run Keyword And Ignore Error` 处理可能失败的前置操作

### 错误处理

> **注意**：关于 BuiltIn 库的错误处理关键字（如 `TRY/EXCEPT`、`Run Keyword And Ignore Error` 等）的使用规范，请参考"编写风格-BuiltIn库使用规范-错误处理"章节。

#### 异常场景验证

- 使用 `Resp Dict Compare` 验证错误返回码和错误消息
- 示例：`Resp Dict Compare    {"ret_code":"V1","ret_msg":"商户号与渠道商户号不存在绑定关系"}`

#### 条件判断

- 使用 `Run Keyword If` 进行条件判断
- 使用 `Set Variable If` 进行条件赋值
- 示例：`${is_exist}    Set Variable If    '${fail_reason}' in ${resoult_message}    ${True}    ${False}`

**`Run Keyword If` 赋值注意事项**：
- `Run Keyword If` 如果要赋值，变量名必须在最前面
- 如果使用同名变量进行条件判断并赋值，在 ELSE 分支中需要使用 `Set Variable ${变量名}` 来承接前面的值，否则变量会被清空
- 示例：
  ```robotframework
  ${suffix_name_list}    Run Keyword If    ${is_list} and ${list_length} == 0    Resp Value    data    ELSE    Set Variable    ${suffix_name_list}
  ${is_black}    Run Keyword If    ${is_black} == ${true}    Set Variable If    "${status}[0][status]" == "2"    ${false}    ${is_black}
  ```

#### V4.0商户系统特殊技巧

**申请单管理**：
- 使用 `随机生成32位申请单号` 或 `V4商户申请单随机生成` 生成唯一申请单号
- 使用 `V4商户申请单状态修改` 在用例失败时作废申请单，避免影响后续执行
- 使用 `Run Keyword If Test Failed` 在用例失败时自动清理

**审核流程处理**：
- 使用 `V4商户通用审核` 进行审核操作（通过/拒绝）
- 使用 `V4工作流ID查询` 查询工作流ID
- 使用 `V4商户变更申请详情查询` 查询变更申请状态

**服务健康检查**：
- 在资料提交类用例中使用 `服务健康检查` 确保服务可用
- 示例：`[Setup]    服务健康检查    ${merch_access_standard_node}`

**数据生成**：
- 使用 `V4商户基础信息生成` 生成商户基础信息
- 使用自定义关键字封装复杂的数据准备逻辑

### 用户关键字编写规范

#### 关键字命名风格

**存量业务关键字**：
- 直接使用业务描述命名，简洁明了
- 示例：`商户详情查询`、`商户业务套餐查询`、`存量终端加机`、`存量终端撤机`

**V4.0商户系统关键字**：
- 以 `V4` 或 `V4商户` 开头，便于区分新旧系统
- 示例：`V4商户状态变更`、`V4商户基本信息变更`、`V4商户申请单随机生成`、`V4工作流ID查询`

#### 关键字编写逻辑

**标准结构**：
1. **`[Documentation]`**：详细说明关键字用途、参数含义、返回值说明
2. **`[Arguments]`**：定义参数，支持默认值和可选参数
3. **`[Return]`**：说明返回值（如有）
4. **实现逻辑**：调用接口、验证响应、设置变量

**参数设计原则**：
- 必填参数放在前面，可选参数放在后面
- 使用默认值减少调用复杂度
- 支持参数替换（`replace_param`）增强灵活性
- 支持响应验证参数（`resp`）允许自定义验证

**变量管理**：
- 使用 `Set Suite Variable` 设置套件级变量，供后续用例使用
- 使用 `Set Test Variable` 设置测试用例级变量，仅在当前用例内可见
- 使用 `[Return]` 返回局部变量供调用方使用
- 变量命名清晰，体现业务含义

**BuiltIn库关键字在关键字编写中的应用**：
- **循环处理**：使用 `FOR` 循环处理列表数据，结合 `Run Keyword And Ignore Error` 处理可能失败的操作
- **条件执行**：使用 `Run Keyword If` 根据条件执行不同关键字，避免复杂嵌套
- **关键字组合**：使用 `Run Keywords` 组合多个关键字，用 `AND` 连接，实现多步骤原子操作
- **变量转换**：使用 `Convert To String`、`Convert To Integer`、`Convert To List` 等进行类型转换
- **列表操作**：使用 `Get Length` 获取列表长度，使用 `Get From List` 获取列表元素，使用 `Append To List` 添加元素
- **字符串操作**：使用 `Get Substring`、`Replace String`、`Split String` 等进行字符串处理
- **等待机制**：使用 `Wait Until Keyword Succeeds` 等待关键字成功执行，适用于异步操作
- **评估表达式**：使用 `Evaluate` 执行 Python 表达式，注意使用 `${{}}` 语法避免变量替换问题

#### 关键字分类和编写技巧

**查询类关键字**：
- 封装查询接口调用
- 验证返回码和返回消息
- 提取响应数据并设置套件变量
- 支持自定义响应验证
- **类型处理技巧**：当使用 `Resp Dict Value` 提取数组字段时，如果只有一个结果可能返回字符串而非列表。判断类型时不能直接在 Python 表达式中使用 `isinstance()`（会导致字符串值被当作变量名），应使用 `Get Length` 和 `Convert To String` 结合的方法：比较变量的长度和其字符串表示的长度，如果长度不相等说明是列表，如果相等说明是字符串。如果不是列表则使用 `Create List` 转换为列表，确保返回值始终是列表类型

```robotframework
商户详情查询
    [Documentation]    商户详情查询
    [Arguments]    ${merch_no}    ${source}=4    ${resp}={"ret_code":"00","ret_msg":"处理成功"}
    [Return]    ${merch_detail_resp_data}
    &{merch_detail_resp_data}    Http Post Api    ${merch_query_url}/querySubMerchDetailRpc    {"merch_no":"${merch_no}","source":"${source}"}
    Resp Dict Compare    ${resp}
    Set Suite Variable    ${merch_detail_resp_data}    ${merch_detail_resp_data}
```

**变更类关键字**：
- 封装变更接口调用
- 支持参数替换（`replace_param`）
- 支持自定义响应验证
- V4.0系统关键字通常需要传入申请单号

```robotframework
V4商户状态变更
    [Documentation]    V4商户状态变更
    [Arguments]    ${param}    ${rep_param}={}    ${resp}={"ret_code":"00000","ret_msg":"Success"}
    Http Post Api    ${merch_access_change_node}${status_update}    ${param}    ${rep_param}
    Resp Dict Compare    ${resp}
```

**数据准备类关键字**：
- 封装复杂的数据生成逻辑
- 组合多个步骤完成数据准备
- 返回生成的数据供后续使用

```robotframework
V4商户基础信息生成
    [Documentation]    V4商户基础信息生成，包含电话、姓名、身份证等
    [Arguments]    ${merch_type}    ${agent_id}=${merch_top_agentid}
    [Return]    ${base_data}
    V4商户申请单随机生成    ${agent_id}    ${merch_type}
    随机生成姓名
    随机生成电话
    随机生成银行卡    ccb_debit19    1
    # ... 更多数据生成步骤
```

**前置清理类关键字**：
- 封装环境清理逻辑
- 使用 `Run Keyword And Ignore Error` 处理可能失败的操作
- 支持循环处理多个数据

```robotframework
商户加机前查询并撤机
    [Documentation]    商户加机前查询并撤机,传入设备SN和状态列表
    [Arguments]    ${device_sn}    ${status_list}
    FOR    ${status}    IN    @{status_list}
        Run Keyword And Ignore Error    Run Keywords    商户终端列表查询    {...}    AND    终端撤机    ${term_no}
    END
```

**套件前置类关键字**：
- 封装套件级别的数据准备和清理
- 使用循环处理批量数据
- 查询数据库获取待处理数据

```robotframework
V4商户变更数据前置
    [Documentation]    V4商户变更数据前置,先结束前N天没有完成的V4商户变更申请单
    [Arguments]    ${days}=5
    通用查询数据库    ...    T.CHANGE_STATUS NOT IN ('3','4','5') ...
    FOR    ${i}    IN    @{return_data}
        Run Keyword And Ignore Error    V4工作流ID查询    {"apply_id":"${i}[apply_id]"}
        V4商户申请单状态修改    ${i}[apply_id]    5
    END
```

#### 关键字业务逻辑封装

**存量业务关键字业务逻辑**：

- **商户查询类**：封装商户详情、业务套餐、结算卡等查询操作。通过调用查询接口获取商户信息，验证返回数据，并将结果设置为套件变量供后续用例使用。
- **终端管理类**：封装终端加机、撤机等操作。加机操作支持参数替换，撤机操作支持根据终端号或商户号查询后撤机。
- **数据查询类**：封装数据库查询操作，支持复杂查询条件，返回查询结果供后续使用。

**V4.0商户系统关键字业务逻辑**：

- **申请单管理类**：封装申请单生成、状态修改等操作。申请单生成时自动生成唯一申请单号并提交，状态修改用于清理未完成的申请单。
- **变更操作类**：封装各类变更操作（状态变更、基本信息变更、身份信息变更等）。变更操作需要传入申请单号，支持参数替换和自定义响应验证。
- **审核流程类**：封装审核相关操作，包括工作流ID查询、通用审核等。审核操作需要先领取任务，再完成审核，支持通过和拒绝两种操作。
- **规则查询类**：封装各类规则查询操作，包括变更规则、报备规则、审核规则等。规则查询用于了解业务规则配置，支持按条件查询。
- **数据生成类**：封装复杂的数据生成逻辑，包括商户基础信息生成、申请单生成等。数据生成时自动调用多个数据生成关键字，组合生成完整的测试数据。

### 核心业务编写规范

#### 入网业务

**存量业务入网模式**：
- **电签入网**：使用 `电签首次入网` 关键字完成入网流程，包含客户号生成、随机数据生成、入网接口调用等步骤
- **营业执照商户入网**：使用 `合伙人营业执照商户首次入网` 关键字，包含mock配置、数据生成、入网接口调用、审核流程等
- **小微商户入网**：使用 `合伙人小微商户入网` 关键字，包含人脸识别、签名、经营附件上传等步骤
- **入网验证**：通过错误返回码验证入网校验规则，如地址验证、经纬度验证、重复入网检测等

**V4.0商户系统入网模式**：
- **申请单生成**：使用 `V4商户申请单随机生成` 生成唯一申请单号，支持商户类型参数（0-小微，1-营业执照）
- **基础信息生成**：使用 `V4商户基础信息生成` 生成完整的商户基础数据，包括加密字段处理
- **资料提交**：按模块顺序提交资料（基本信息→身份信息→经营信息→结算信息→其他补充材料）
- **入网规则校验**：提交资料前查询入网规则，确保资料符合要求
- **审核流程**：提交审核后查询审核状态，支持自动审核和人工审核

**入网业务编写技巧**：
- 使用 `服务健康检查` 确保服务可用（V4.0系统）
- 使用 `修改新mock配置` 配置第三方接口mock数据
- 使用 `V4商户部分字段加密` 处理敏感字段加密
- 入网后需要等待数据同步，通常等待 `3-5` 秒
- 使用 `商户数据迁移OB` 完成数据迁移（存量业务）

#### 变更业务

**存量业务变更模式**：
- **直接变更**：直接调用变更接口，修改后查询验证，最后恢复数据
- **变更验证**：通过查询接口和数据库查询双重验证变更结果
- **数据恢复**：变更验证完成后立即恢复原始数据，确保不影响后续用例

**V4.0商户系统变更模式**：
- **申请单管理**：变更前创建变更申请单，使用 `V4商户创建变更申请单` 或直接使用 `随机生成32位申请单号`
- **审核信息获取**：变更前查询审核人员信息，使用 `V4查询商户变更审核规则` 获取审核配置
- **变更提交**：调用变更接口提交变更申请，传入申请单号和审核信息
- **审核处理**：等待自动审核或使用 `V4商户通用审核` 进行人工审核
- **状态查询**：使用 `V4商户变更申请详情查询` 和 `V4工作流ID查询` 查询变更状态
- **申请单清理**：用例失败时使用 `V4商户申请单状态修改` 作废申请单

**变更业务编写技巧**：
- 使用 `V4商户变更数据前置` 在套件级别清理未完成的申请单
- 使用 `Run Keyword If Test Failed` 在用例失败时自动清理申请单
- 变更操作通常需要等待 `10` 秒（自动审核时间）
- 使用 `指定商户号添加商户黑名单表记录` 准备黑名单测试数据

#### 查询业务

**查询业务通用模式**：
- **直接查询**：调用查询接口，验证返回码和返回消息
- **数据验证**：验证返回数据不为空或符合预期
- **JSONPath验证**：使用JSONPath表达式验证特定字段值

**查询业务编写技巧**：
- 查询类关键字通常设置套件变量，供后续用例使用
- 使用 `Resp Dict Compare` 配合JSONPath一次性完成多字段校验
- 使用 `Should Not Be Empty`、`Should Not Be Equal` 等断言验证数据存在性
- 查询操作通常不需要等待，直接验证结果

#### 加机业务

**存量业务加机模式**：
- **前置清理**：使用 `商户加机前查询并撤机` 清理环境，传入设备SN和状态列表
- **执行加机**：调用 `存量终端加机` 关键字，支持参数替换
- **验证结果**：查询终端列表验证终端状态
- **清理工作**：使用 `[Teardown]` 调用 `存量终端撤机` 确保环境恢复

**V4.0商户系统加机模式**：
- **前置清理**：使用 `[Setup]` 中的 `Run Keyword And Ignore Error` 清理环境
- **申请单生成**：使用 `V4商户申请单随机生成` 生成申请单号
- **执行加机**：调用加机接口，传入申请单号、终端SN、终端型号、终端类型等参数
- **验证结果**：验证返回码和返回消息，提取终端号
- **立即清理**：加机完成后立即撤机，使用 `终端撤机` 关键字

**加机业务编写技巧**：
- 加机前必须清理环境，避免重复加机导致用例失败
- 加机后需要等待 `3` 秒再撤机，确保数据同步完成
- 使用 `终端撤机` 关键字支持根据终端号或商户号撤机
- V4.0系统加机需要传入申请单号，支持多种终端类型（码牌、电子码牌、传统POS、智能POS、码付等）

#### 报备业务

**存量业务报备模式**：
- **报备接口调用**：调用各渠道报备接口（银联、连通、微信、支付宝等）
- **数据库验证**：查询数据库验证报备记录是否写入
- **报备记录验证**：验证数据库记录不为空，确认报备成功

**V4.0商户系统报备模式**：
- **报备接口调用**：调用商户入网报备接口，传入商户号和报备人员
- **报备规则查询**：使用 `V4商户渠道报备规则查询` 和 `V4终端渠道报备规则查询` 查询报备规则
- **实名认证**：微信和支付宝渠道需要先进行实名认证申请和查询
- **报备失败重试**：支持报备失败后重新发起报备操作

**报备业务编写技巧**：
- 报备操作通常需要等待 `3-5` 秒再查询数据库验证
- 使用 `通用查询数据库` 验证报备记录写入数据库
- 微信报备需要先查询门店信息获取shopId
- 报备操作通常不需要恢复，因为报备记录是业务数据

#### 资料提交业务

**V4.0商户系统资料提交模式**：
- **申请单生成**：使用 `V4商户申请单随机生成` 生成申请单号
- **基础信息生成**：使用 `V4商户基础信息生成` 生成完整的商户基础数据
- **按模块提交**：按顺序提交各类资料（基本信息→身份信息→经营信息→结算信息→其他补充材料）
- **规则校验**：提交前查询入网规则，确保资料符合要求
- **审核提交**：资料提交完成后提交审核，触发审核流程

**资料提交业务编写技巧**：
- 使用 `服务健康检查` 确保服务可用
- 使用 `V4商户部分字段加密` 处理敏感字段
- 支持不同签约方式（纸质协议P、电子协议E、承诺函L）
- 支持不同产品类型（标准POS、扫码POS、SAAS、场景POS）
- 支持不同结算类型（法人结算、企业结算、非法人结算等）
- 资料提交后需要等待数据同步，通常等待 `3-5` 秒

#### 进件典型场景业务

**进件典型场景模式**：
- **完整流程封装**：使用复合关键字封装完整的进件流程
- **多步骤组合**：组合资料提交、审核、同步等多个步骤
- **场景差异化**：不同产品类型和主体类型使用不同的关键字组合

**进件典型场景编写技巧**：
- 使用 `V4营业执照商户企业类型提交资料` 等复合关键字简化用例编写
- 进件前需要mock鉴权接口，使用 `V4工商照面鉴权mock` 等关键字
- 进件流程需要查询审核规则和审核人员信息
- 进件后需要等待数据同步，使用 `商户数据迁移OB` 完成数据迁移
- 支持重复入网场景，验证重复入网的业务规则

#### 规则查询业务

**规则查询业务模式**：
- **入网规则查询**：查询入网资料提交规则、审核规则、审核人员等
- **变更规则查询**：查询变更规则、变更审核规则、变更报备规则等
- **报备规则查询**：查询商户渠道报备规则、终端渠道报备规则等

**规则查询业务编写技巧**：
- 使用 `V4查询商户变更规则`、`V4查询商户变更审核规则`、`V4查询商户变更报备规则` 等关键字
- 规则查询通常需要传入产品类型、主体类型、变更类型等参数
- 规则查询结果用于了解业务规则配置，指导后续操作
- 使用 `Lists Should Be Equal` 验证规则列表数据（V4.0系统）

### 商户系统业务描述

#### 商户变更业务

**存量业务**：
- **核心数据变更**：商户基本信息、业务套餐绑定、授权收款套餐等核心数据的变更操作。通过直接调用变更接口修改数据，然后查询验证修改结果，最后恢复原始数据。
- **客户信息相关**：客户定期识别、重新识别等客户信息管理操作。通过触发识别流程，查询识别记录，验证识别结果。
- **无交易处置**：商户无交易情况下的处置流程。根据无交易规则对商户进行停用或注销处理。

**V4.0商户系统**：
- **商户状态变更**：商户正常、停用、注销状态的变更操作。通过创建变更申请单，提交状态变更申请，系统根据审核规则自动审核或人工审核，变更成功后验证商户状态。
- **基本信息变更**：商户名称、联系方式、地址等基本信息的变更操作。通过变更申请单提交变更，审核通过后生效。
- **身份信息变更**：商户法人、证件信息等身份相关信息的变更操作。需要提交变更申请并经过审核流程。
- **经营信息变更**：商户经营范围、MCC码等经营相关信息的变更操作。
- **结算信息变更**：商户结算卡、结算周期等结算相关信息的变更操作。
- **结算费率变更**：商户交易费率的变更操作，需要审核通过后生效。
- **产品类型升级/降级**：商户产品类型的变更操作，如从标准POS升级到扫码POS。
- **变更规则执行**：根据商户变更规则自动执行相应的变更操作和报备流程。

#### 商户查询业务

**存量业务**：
- **商户查询服务**：查询商户详情、渠道商户信息、客户信息等各类查询服务。直接调用查询接口，验证返回数据不为空或符合预期。
- **核心数据查询**：查询商户核心业务数据，如业务套餐、授权收款套餐等信息。

**V4.0商户系统**：
- **基本信息查询**：查询商户基本信息，包括商户名称、联系方式、地址等基础数据。
- **身份信息查询**：查询商户身份信息，包括法人信息、证件信息等。
- **经营信息查询**：查询商户经营信息，包括经营范围、MCC码等。
- **结算信息查询**：查询商户结算信息，包括结算卡、结算周期等。
- **变更申请查询**：查询商户变更申请列表和详情，包括申请状态、审核进度等。
- **渠道商户号查询**：查询商户在各渠道的商户号信息，如银联、微信、支付宝等。

#### 商户报备业务

**存量业务**：
- **商户报备**：银联、连通、微信、支付宝等渠道的商户报备操作。调用报备接口后，查询数据库验证报备记录。
- **终端报备**：终端设备的报备操作，验证报备记录是否写入数据库。
- **商户注销报备**：商户注销相关的报备流程，确保注销信息正确报备到各渠道。

**V4.0商户系统**：
- **商户入网报备**：商户入网时向各渠道报备商户信息。根据商户类型（营业执照、小微商户等）进行相应的报备操作。
- **加机终端报备**：终端加机后向渠道报备终端信息，确保终端在各渠道正常使用。
- **微信实名认证**：微信渠道的实名认证申请和查询，包括申请提交和认证状态查询。
- **支付宝实名认证**：支付宝渠道的实名认证申请和查询。
- **特约商户上报**：向监管机构上报特约商户信息。
- **报备规则查询**：查询商户和终端的渠道报备规则，了解报备条件和要求。
- **报备失败重试**：当报备失败时，支持重新发起报备操作。

#### 终端管理业务

**存量业务**：
- **终端加机**：电签商户、营业执照商户、小微商户的终端加机操作。加机前清理环境，加机后验证终端状态，最后清理环境。
- **终端换机**：终端设备的更换操作，验证换机后终端状态。
- **终端查询**：终端列表、终端状态等查询操作。

**V4.0商户系统**：
- **终端加机**：支持多种终端类型的加机操作（码牌、电子码牌、传统POS、智能POS、码付等）。通过申请单号管理加机流程，加机完成后立即撤机清理环境。
- **终端撤机**：终端设备的撤机操作，确保终端从商户解绑。
- **终端换机**：终端设备的更换操作，将旧终端替换为新终端。
- **终端变更**：终端信息的变更操作，如终端名称、终端类型等。
- **终端状态变更**：终端状态的变更操作，如启用、停用等。

#### 商户入网业务

**存量业务**：
- **电签入网**：电签商户的首次入网、重复入网、门店认证等入网流程。包括入网信息提交、门店认证、银联绑定等步骤。
- **入网验证**：地址验证、经纬度验证、重复入网检测等入网校验。验证入网信息的合法性和准确性。

**V4.0商户系统**：
- **资料提交**：商户入网时提交各类资料，包括基本信息、身份信息、经营信息、结算信息等。支持不同签约方式（纸质协议、电子协议、承诺函）。
- **入网规则校验**：提交资料时进行入网规则校验，确保资料符合入网要求。
- **入网规则执行**：根据入网规则自动执行相应的操作，如生成商户号、创建审核流程等。
- **审核流程**：查询入网审核规则、审核人员、资料提交规则等，支持审核通过或拒绝操作。
- **进件流程典型场景**：标准POS、扫码POS、SAAS、场景POS等不同产品类型的进件流程。

## 持续优化模块

### 文档更新原则

每次添加新用例后，需要及时更新本文档，记录以下内容：

1. **逻辑优化**：记录用例编写中的逻辑思路和最佳实践
   - 复杂业务场景的处理方式
   - 数据流转和依赖关系的设计思路
   - 异常情况的处理策略

2. **技巧总结**：提炼可复用的编写技巧
   - 高效的数据提取方法
   - 断言验证的优化方式
   - 用例结构化的组织方式

3. **方法改进**：记录测试方法的改进点
   - 接口调用的优化模式
   - 测试数据准备的高效方式
   - 用例维护的便捷方法

### 更新要求

- **不包含用例代码**：只记录逻辑、技巧和方法，不粘贴完整用例
- **不包含变量定义**：不记录具体的变量名和变量值
- **示例简洁**：如必须提供示例，仅展示关键代码片段，保持简洁明了
- **及时更新**：每次添加用例后立即更新，避免积累过多内容
- **文档整理**：每次修改文档后必须进行整体整理，检查并消除重复内容，优化不当描述，确保文档结构清晰、内容精炼、无冗余

### 文档整理检查清单

每次修改文档后，按以下清单检查并优化：

1. **重复内容检查**：
   - 检查是否有相同或相似的内容出现在多个章节
   - 合并重复内容，保留最完整、最准确的描述
   - 使用交叉引用替代重复描述

2. **描述优化**：
   - 检查描述是否准确、清晰
   - 优化模糊或不当的描述
   - 统一术语和表达方式

3. **结构优化**：
   - 检查章节结构是否合理
   - 确保内容层次清晰
   - 相关内容归类到对应章节

4. **示例优化**：
   - 检查示例是否简洁明了
   - 删除冗余示例
   - 确保示例与描述一致

### 示例格式

**好的记录方式：**
- 使用 JSONPath 过滤时，优先使用多条件组合（`&&`、`||`）而非多次调用
- 对于列表数据验证，使用 `Resp Dict Compare` 配合 JSONPath 一次性完成多字段校验

**避免的记录方式：**
- 不要记录：`${merch_no}    Set Variable    M123456789`（具体变量）
- 不要记录：完整的测试用例代码块

## DateTime 库使用规范

### 日期时间获取

- `Get Current Date` - 获取当前日期时间
- `Get Time` - 获取当前时间戳
- 示例：
  ```robotframework
  ${current_date}    Get Current Date
  ${timestamp}    Get Time    epoch
  ```

### 日期时间格式化

- `Convert Date` - 转换日期格式
- `Get Time` - 获取格式化时间
- 示例：
  ```robotframework
  ${formatted}    Convert Date    ${date}    result_format=%Y-%m-%d
  ${time_str}    Get Time    format=%Y-%m-%d %H:%M:%S
  ```

### 日期时间计算

- `Add Time To Date` - 添加时间到日期
- `Subtract Date From Date` - 计算日期差
- 示例：
  ```robotframework
  ${future_date}    Add Time To Date    ${date}    1 day
  ${diff}    Subtract Date From Date    ${date1}    ${date2}
  ```

## 技术技巧总结

### 变量替换

- 在自定义库中使用 `BuiltIn.replace_variables()` 处理变量替换
- 支持访问当前可用变量并处理字符串中包含的变量表达式（如 `${VAR}`）
- 适用于需要动态解析变量的场景

### 返回值处理

- Robot Framework 5+ 推荐使用 `RETURN` 语法返回值
- 兼容旧版本时，可使用 `[Return]` 或 `Return From Keyword`
- 支持返回多个值，用多个空格分隔

### 关键字复用

- 封装通用逻辑为关键字，提高代码复用性
- 使用参数和默认值增强关键字灵活性
- 支持参数替换（`replace_param`）和响应验证参数（`resp`）

### 性能优化

- 避免不必要的等待时间
- 合理使用变量作用域，减少内存占用
- 使用 `Run Keywords` 组合原子操作，减少关键字调用次数

### 可维护性

- 使用清晰的命名，体现业务含义
- 添加必要的注释，说明业务逻辑
- 保持代码结构清晰，避免过度嵌套
- 及时更新文档，记录最佳实践

## 最佳实践总结

### 用例编写

1. **结构清晰**：每个用例包含完整的标签和清晰的步骤
2. **描述准确**：Documentation 标签准确描述业务场景
3. **验证充分**：每个用例包含明确的验证点
4. **清理及时**：使用 Teardown 确保环境恢复

### 关键字编写

1. **命名规范**：使用中文描述，清晰表达功能
2. **参数设计**：必填参数在前，可选参数在后，支持默认值
3. **错误处理**：合理使用错误处理机制，提高健壮性
4. **文档完善**：详细说明参数和返回值

### 变量管理

1. **作用域选择**：优先使用局部变量，谨慎使用全局变量
2. **命名规范**：使用有意义的名称，体现业务含义
3. **类型明确**：明确变量类型，避免类型混淆

### 代码质量

1. **可读性**：代码清晰，注释充分
2. **可维护性**：结构合理，易于修改
3. **可复用性**：封装通用逻辑，提高复用
4. **健壮性**：错误处理完善，异常场景覆盖